import { ShowifloggedinDirective } from './showifloggedin.directive';

describe('ShowifloggedinDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowifloggedinDirective();
    expect(directive).toBeTruthy();
  });
});
